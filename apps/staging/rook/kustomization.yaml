apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  interval: 5m
  path: "../../apps/base/rook-operator/"
  prune: true
  sourceRef:
    kind: GitRepository
    name: rook-ceph