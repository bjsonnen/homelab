apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: falcosidekick
  namespace: falco
spec:
  interval: 1m
  chart:
    spec:
      chart: falcosidekick
      version: "2.31.1"
      sourceRef:
        kind: HelmRepository
        name: falco
        namespace: falco
  targetNamespace: falco
  values:
    replicaCount: 1
